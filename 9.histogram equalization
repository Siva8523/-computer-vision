import cv2
import matplotlib.pyplot as plt
image = cv2.imread("C://Users//venna//OneDrive//Pictures//Screenshots//elephnat.jpeg", cv2.IMREAD_GRAYSCALE)
equilized_image = cv2.equalizeHist(image)
plt.figure(figsize=(10, 5))
plt.subplot(1, 2, 1)
plt.imshow(image, cmap='gray')
plt.title('Original Image')
plt.axis('off')
plt.subplot(1, 2, 2)
plt.imshow(equilized_image, cmap='gray')
plt.title('Histogram Equalized Image')
plt.axis('off')
plt.tight_layout()
plt.show()
